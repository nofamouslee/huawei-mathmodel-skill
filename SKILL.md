---
name: huawei-mathmodel
description: "华为杯（中国研究生数学建模竞赛）分阶段辅助：比较赛题、逐项拆题、审计数据、建立数学模型、编程求解、实验验证、论文级实验分析图、GPT Image 框架/流程图提示词、Markdown 论文与结果审查。用户要求指定阶段或任务、清晰的 plan/todo、续接建模项目、结果复核或数模论文时使用。默认完成本轮指定范围后停止；计划内实验及必要验证完成后才开始论文写作。不依赖论文模板或排版引擎。"
---

# 华为杯数学建模工作流

帮助队伍形成符合题意、数学合理、可运行、可验证、可追溯的完整答案。默认使用中文解释，先说明专业术语。以真实证据判断进度，不承诺获奖、全局最优或没有经过验证的效果。

## 固定交付与执行原则

- 最终论文输出 paper/paper.md。使用 Markdown 标题、表格、相对图片路径和 LaTeX 数学语法。数学公式语法不意味着使用 LaTeX 排版。
- 不引入竞赛论文模板，不询问 Typst/LaTeX，不安装排版工具，不生成 PDF、Word 或封面，除非用户以后明确另行要求。
- 先建立清晰的 plan.md 与 todo.md，再进入实质工作；已有项目先读现状再续接。简单方法咨询可直接回答，不强行初始化项目。
- 对正式比赛读取当届官方规则、赛题更正和 AI 使用规定；由队员承担答题主体工作。保留真实 AI 使用记录。公开资料检索与队外赛题交流分开处理。
- 在本轮指定范围内持续完成已授权的读取、实现、试算、修复和验收，不反复索要确认。到达本轮停止点后保存进度并结束，不擅自进入下一阶段。
- 保留用户已定偏好、题号、软件和资源限制；不擅自更换任务、数据划分或最终评价口径。
- 不默认启动多个代理。按当前会话授权与环境能力执行；单一执行者也必须能完成全部流程。
- 默认分阶段执行。实验完成前仅保存题意、模型、文献和实验记录；全部计划内实验及必要验证完成后才进入 S6 写论文，不提前生成论文正文、摘要或占位稿。

## 入口与按需阅读

首先判断请求范围：完整解题、选题/分析、实现、写作、审查或续接。只执行当前范围所需工作，不把缺失附件虚构为已读取。

| 情况 | 必须阅读 |
| --- | --- |
| 启动、续接、调整任务、时间不足 | references/planning.md |
| 选题、拆题、数据审计、阶段推进 | references/workflow.md |
| 模型选型、数学检查、实验设计 | references/modeling.md |
| 编程运行、登记结果、数值引用、验收 | references/evidence.md |
| 论文、图表、引用、Markdown 交付 | references/writing.md |
| 实验分析图、统一绘图风格、GPT Image 框架/流程图提示词、图形验收 | references/figures.md |

通过当前环境提供的 skill 路径定位 scripts/，不要假定安装目录固定。初始化工作项目使用：

~~~bash
python3 <技能目录>/scripts/project.py init --root <项目目录> --title <项目名> --mode practice --scope full
~~~

mode 可选 practice、competition、unknown。仅用户或当前题目明确属于训练时选择 practice；不确定就记录 unknown。年份、截止时间和剩余算力未知时保留未知，不能假定固定100小时或可用GPU。

scope 可选 full、analysis、coding、writing、review。续接时不要重新初始化或覆盖文件。项目保存位置遵循当前环境规则；在 ChatGPT Work 中交付项目文件时使用 Library，在外部代码项目中遵循该项目存储方式。技能本体不放进比赛项目。

## 本轮执行边界

- 区分项目的总范围scope与本轮执行范围。scope=full表示项目最终目标覆盖全部流程，不代表本轮必须一路做到论文交付。
- 支持指定一个阶段、连续几个阶段，或某一问/某几个任务，例如“只做S2”“做到S3后停止”“只完成问题二的基线”。在plan.md写清本轮目标、题号/任务ID、停止点和交付物，再从todo选择对应任务。
- 未给出范围时默认完成当前未完成阶段；新项目从S0开始。用户已明确要求完成一组工作时，以这组工作为边界；只有明确要求连续完成全流程才连续推进到交付。
- “继续”表示续做当前未完成阶段；该阶段已通过时进入下一个依赖满足的阶段，完成后停止。不得重新初始化或重复有效实验。
- 单阶段执行前可以读取和核验必要上游成果；上游缺失时记录阻塞和最小补齐任务，不擅自把整个项目全部重跑。当前必要依赖未满足不得强行执行下游任务。
- 本轮结束时更新plan、state和todo，报告阶段完成情况、证据、未决问题与建议的下一步。阶段完成不等于全项目完成，后续任务继续保留TODO/BLOCKED。
- S6另有写作前提：核对全部计划内实验与必要验证已完成、G4/G5依据有效，且本轮范围包含写作。具体检查见references/writing.md。

## 九个阶段与通过条件

阶段按 S0—S8 编号，检查按 G0—G8 编号。读 references/workflow.md 获取行动、产出和回退方式。

| 阶段 | 核心行动 | 主要产出 | 通过条件 |
| --- | --- | --- | --- |
| S0 接收与规划 | 确认任务范围、规则、材料、资源、已有进度 | plan.md、todo.md、state.json、reports/intake.md | G0：范围和关键输入明确，预算与阻塞项已记录 |
| S1 选题与拆解 | 必要时比较候选题；逐项提取要求与依赖 | reports/requirements.md、state.json 中 requirements | G1：全部顶层问题及具体要求均被覆盖 |
| S2 数据与假设检查 | 读附件、核对单位、检查歧义、确定划分 | reports/data_assumptions.md、可追溯数据处理 | G2：数据/条件可用，关键歧义有依据或明确条件分支 |
| S3 建模与验证设计 | 形式化变量、关系、约束、求解和评价协议 | reports/modeling.md | G3：模型符合题意、可实现，验证与停止条件明确 |
| S4 基础实现 | 小实例检查、逐问运行、建立基线 | code/、results/、运行记录 | G4：关键路径跑通，约束/边界与基础结果检查通过 |
| S5 改进与验证 | 有依据地改进；公平比较、误差及稳健性检查 | reports/results.md、实验记录、数据图 | G5：主要结论获得相应证据，局限如实记录 |
| S6 Markdown 论文 | 根据结果组织逐问论证、图表、摘要与引用 | paper/paper.md、figures/ | G6：答题完整，数字与依据一致，Markdown 可读 |
| S7 最终验收 | 数学、实现、题意、文稿分别复核 | reports/review.md、reports/audit.json | G7：必要审查通过，无遗漏、失效结果或未决硬错误 |
| S8 交付归档 | 核对交付清单，冻结对应版本，报告真实状态 | reports/delivery.md、delivery_manifest.json | G8：Markdown 与支持材料齐全、引用路径有效 |

正式比赛中的官方提交状态单独记录；Markdown 交付通过不等于符合某年官方上传格式或已经提交。用户当前只要求 Markdown 时，到 Markdown 交付与归档结束。

## 每次执行的固定循环

1. 读取用户最新约束、plan.md、state.json、todo.md 和当前阶段证据。
2. 对照真实文件检查已完成状态，识别新输入、变更及失效的下游结果。
3. 从 todo 选择本轮范围内依赖满足且最影响当前目标的任务；优先覆盖漏答要求和关键路径，不越过停止点。
4. 说明这一步做什么、为何做、怎样判断通过；按预算完成实际工作。
5. 记录输入/输出、运行状态、证据和发现；结果失败时保存失败记录，不改写成成功。
6. 更新 state.json 的任务与要求状态；完成任务必须有证据路径。更新 plan.md 的决策与回退记录。
7. 运行 project.py sync 更新 todo.md；检查当前阶段条件，在本轮边界内进入下一任务或回退。达到停止点则结束本轮。
8. 反馈本轮已完成、关键结果、剩余问题与建议下一步，保留后续未执行任务。仅缺失会改变关键判断的信息才向用户提问。

plan.md 回答“整体为什么这样做”；todo.md 回答“现在具体做什么”；state.json 保存任务、要求、检查和实验的结构化状态。三者必须一致。不要仅维护粗略阶段复选框。

## 任务与状态约束

- 分开记录“问题二”这样的论文章节与 Q2.a、Q2.b 这样的具体要求。无编号要求也要登记，不固定三问。
- 仅分析规划时单独记录每项要求的analysis状态与证据；题目status保留实际解答状态。完成方案不等于完成预测或求解，具体规则见planning.md。
- 将任务写成可验收动作，指定阶段、关联要求、依赖、优先级、预计耗时、产出、完成标准、状态、证据、阻塞原因。
- 使用 TODO、DOING、BLOCKED、DONE、SKIPPED、STALE。DONE 必须有完成证据；必要任务不可用 SKIPPED 绕过。
- 使用 PASS、FAIL、NOT_RUN、BLOCKED、NA 记录检查。PASS 要有带摘要校验的依据；NA 要说明真实不适用原因。必要检查未执行不得整体通过。
- 任务依赖发生实质变化时把受影响的完成任务设为 STALE，重新检查相关图表和论文，不无条件重跑所有工作。
- 对时间紧张的项目减少候选数量、搜索预算与装饰性图示，保留任务覆盖、核心可行性/误差检查和真实结论。

## 论文绘图的两条路径

- 实验分析图必须用真实数据和代码生成；按 references/figures.md 统一图型、统计含义、字体、配色、布局和导出，保留输入、代码、图注与实际视觉检查证据。禁止用生成式图像制作实验曲线或数值图。
- 论文框架图与流程图默认交付 GPT Image 提示词包：实际节点/连线、完整提示词、精确标签、图注建议和验收清单；不自动调用图像生成，不把提示词标记为成图。图示必须符合实际方法，不虚构模块或反馈。
- 图表任务独立写入 plan/todo，并遵守本轮阶段边界。S5 可生成真实分析图；论文正文和最终方法图的整合仍须满足 S6 前提。

## 证据与辅助脚本

辅助脚本使用 Python 3.9 及以上版本，仅依赖标准库；不运行模型、不安装依赖、不提交比赛作品。

~~~bash
# 根据已更新的 state.json 生成清晰待办
python3 <技能目录>/scripts/project.py sync --root <项目目录>

# 仅在真实运行结束后登记输入、输出和日志快照
python3 <技能目录>/scripts/project.py record-run --root <项目目录> --id E001 --status SUCCEEDED --kind execution --command "python code/q1.py" --input code/q1.py --input data/clean.csv --output results/e001.json --log results/e001.log

# 完成实际审查后登记检查结论与证据
python3 <技能目录>/scripts/project.py record-check --root <项目目录> --id G4 --status PASS --note "小实例与原始约束逐项复核通过" --evidence reports/baseline_check.md --input code/q1.py --input results/e001.json

# 检查结构、依赖、证据版本、逐项数值绑定和 Markdown 图片路径
python3 <技能目录>/scripts/audit.py --root <项目目录> --require-complete --output reports/audit.json

# 所需条件通过后冻结当前交付文件快照
python3 <技能目录>/scripts/audit.py --root <项目目录> --freeze
~~~

脚本的通过仅证明它实际检查的记录、文件与数字对应关系。数学正确性、数据划分合理性、因果解释、模型适用性必须通过审查证据判断，不能把脚本输出称为自动证明。

按 references/evidence.md 定义登记每个关键数值的源实验、JSON位置、单位与场景，使用 Markdown 注释绑定其显示值。检查零、负数、小于1的数值及所有已登记结论，不使用“出现任意一个数字就通过”的规则。

## 完成与交付

在用户本轮要求范围内完成必要任务和检查后报告阶段完成，并交付对应分析、代码或结果；非写作阶段不要求生成paper/paper.md。论文与完整项目交付时提供paper/paper.md，需要复现时同时交付代码、结果与图表，以及plan/todo和验收摘要。明确解决了哪些问题、完成了哪些验证、主要局限和文件入口。保留必要AI声明；不把内部任务台账塞进论文正文。

没有实际运行、未读取附件、缺少必要证据、未执行必要审查或存在过期结果时，如实报告具体缺口。允许交付明确标注的分析稿或中间成果，但不称为完整已验收论文。
